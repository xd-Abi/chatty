{
  "name": "chatty",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apis/*",
    "client"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "yarn build && turbo run dev"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  }
}
